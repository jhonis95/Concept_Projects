// elements and functions that will manipulate the DOM
// this object element will have all the css classes
// for we can use when we need to show or hide something

export const elements={
    profile: document.querySelector(".profile"),
    input: document.querySelector("input"),
    repos: document.querySelector(".repo-list"),
};
